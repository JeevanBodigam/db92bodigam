//- extends layout

//- block content
//-   h1= title
//-   p Welcome!! #{title}
//-   button(type="button", class="btn btn-secondary btn-lg" onclick="update()") Press me
//-   div(id="target" class="container text-center m-2 p-3" style="color:brown;font-weight:700; background-color:lightgrey;") Display
//-   script. 
//-       var client_count=0 
//-       function update(){
//-         client_count++;
//-         document.getElementById("target").innerHTML=`Value now ${client_count}`
//-       } 
//-   a(href="/fishes") go to fishes


extends layout
block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/fishes") Take me to fishes
    br
    a(href="/logout") Logout